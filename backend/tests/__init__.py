"""Backend test package marker.

This ensures `tests.*` imports resolve to the repo's backend test package
instead of any unrelated third-party `tests` package installed in the
active Python environment.
"""
