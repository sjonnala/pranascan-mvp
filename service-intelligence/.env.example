DATABASE_URL=postgresql+asyncpg://pranascan:pranascan_dev_password@localhost:5432/pranascan
# Keep this false when using Alembic-managed local databases.
# AUTO_CREATE_TABLES=false
ENVIRONMENT=development
DEBUG=false

# Internal service-to-service boundary (service-core -> service-intelligence)
# INTERNAL_SERVICE_TOKEN=dev-internal-service-token
# Optional compute tuning:
# MIN_LIGHTING_SCORE=0.4
# MIN_MOTION_SCORE=0.95
# MIN_FACE_CONFIDENCE=0.8
# MIN_AUDIO_SNR_DB=15.0
